extends layouts/main.pug

block content
    .user-profile
        .user-profile__header 
            img.avatar(src=user.avatarUrl)
            h4.profile__username=user.name
            h5.profile__email=user.email